# backloop

Simple Python backtesting framework. Single-file library (`main.py`) with a `Strategy` + `Backtester` pattern.

## Package manager

Use `uv` for all dependency and script management.

## Linting / type-checking

Run after every change:

```
uvx ruff check .
uvx ty check .
```
